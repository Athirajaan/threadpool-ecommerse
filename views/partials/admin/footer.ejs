<!-- views/partials/footer.ejs -->
</div> <!-- Close the flex container -->

<!-- JavaScript for Sidebar Toggle and Active Link Highlighting -->
<script>
    const menuToggle = document.getElementById('menu-toggle');
    const sidebar = document.getElementById('sidebar');

    menuToggle.addEventListener('click', () => {
        sidebar.classList.toggle('hidden');
    });

    const currentPath = window.location.pathname;
    const sidebarLinks = document.querySelectorAll('#sidebar a');

    sidebarLinks.forEach(link => {
        if (currentPath.includes(link.getAttribute('href'))) {
            link.classList.add('bg-yellow-100', 'text-yellow-500', 'font-semibold');
        } else {
            link.classList.remove('bg-yellow-100', 'text-yellow-500', 'font-semibold');
        }
    });
</script>
</body>
</html>