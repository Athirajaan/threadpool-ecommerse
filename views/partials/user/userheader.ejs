<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>ThreadPool Homepage</title>
  <script src="https://cdn.tailwindcss.com"></script>
    <link href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/5.15.4/css/all.min.css" rel="stylesheet">
    <script src="https://cdn.jsdelivr.net/npm/sweetalert2@11"></script>
   

</head>

</head>
<body class="bg-gray-50 font-sans antialiased">


<!-- Header -->
<header class=" bg-white p-1 relative z-20">
    <div class="container mx-auto flex justify-between items-center px-4 md:px-10">
      <!-- Left Side: Logo -->
      <div class="flex items-center space-x-4">
        <img src="/images/logo.jpg" alt="Logo" class="h-10">
        <a href="/">
          <div>
            <h1 class="text-xl md:text-2xl font-bold text-gray-800 font-serif">
              Thread<span class="text-yellow-500">p</span>ool
            </h1>
            <p class="text-sm font-medium text-gray-500">Strength — Fashion</p>
          </div>
        </a>
      </div>
  
      <!-- Center: Navigation Links -->
      <div class="hidden md:flex items-center justify-center space-x-6 mx-6">
        <a href="/womenShop" class="text-gray-700 font-bold text-lg font-sans">WOMEN'S</a>
        <a href="/menShop" class="text-gray-700 font-bold text-lg font-sans">MEN'S</a>
        <a href="/about" class="text-gray-700 font-bold text-lg font-sans">ABOUT</a>
      </div>
  
      <!-- Right Side: Search Bar, Wishlist, Cart, Profile and Logout -->
      <div class="flex items-center space-x-4">
        <!-- Search Bar -->
        <input type="text" placeholder="Search..." class="px-4 py-2 border rounded-lg w-72 text-sm">
  
        <!-- Wishlist Icon (now after the search bar) -->
        <a href="#" class="text-gray-800">
            <i class="fas fa-heart mr-3"></i>
        </a>
  
        <!-- Cart Icon (Shopping Cart) -->
        <a href="/cart" class="text-gray-800">
          <i class="fas fa-shopping-cart mr-3"></i>
        </a>
  
        <!-- Profile Icon -->
        <% if (isLoggedIn) { %>
          <!-- Display LOGOUT button if user is logged in -->
          <a href="/profile" class="text-gray-800">
            <i class="fas fa-user mr-3"></i>
          </a>
        <% } else { %>
          <!-- Display SIGN IN button if user is not logged in -->
          <button onclick="window.location.href='/signup'" class="py-2 px-4 bg-yellow-400 text-black rounded">
            SIGN IN
          </button>
        <% } %>
      </div>
    </div>
  
    <!-- Mobile Version -->
   
      
  </header>
  
  
 

