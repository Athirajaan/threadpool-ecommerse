<script>
  tailwind.config = {
    theme: {
      extend: {
        fontFamily: {
          sans: ['Inter', 'sans-serif'],
        },
      },
    },
  };
</script>
<!-- Sidebar -->
<aside
  class="w-64 fixed left-0 top-[2rem] bottom-0 bg-white shadow-xl overflow-y-auto z-1"
>
  <!-- Profile Section with softer gradient -->
  <div
    class="relative h-48 bg-gradient-to-br from-amber-100 via-amber-200 to-yellow-300 flex items-center justify-center"
  >
    <div class="absolute inset-0 bg-black opacity-5"></div>
    <!-- Profile Image Container -->
    <div class="relative z-10 flex flex-col items-center">
      <div class="w-24 h-24 rounded-full bg-white p-1 shadow-lg">
        <div
          class="w-full h-full rounded-full bg-gradient-to-br from-gray-700 to-gray-800 flex items-center justify-center"
        >
          <i class="fas fa-user text-white text-3xl"></i>
        </div>
      </div>
    </div>
  </div>

  <!-- User Info - Overlapping Card -->
  <div class="relative -mt-12 mx-4">
    <div
      class="bg-white rounded-xl shadow-md p-6 text-center border border-gray-100"
    >
      <h3 class="text-xl font-bold text-gray-800 mb-1"><%= user.name %></h3>
      <p class="text-sm text-gray-500"><%= user.email %></p>
    </div>
  </div>

  <!-- Navigation Menu -->
  <nav class="mt-6 px-4">
    <ul class="space-y-2">
      <!-- My Info -->
      <li>
        <a
          href="/profile"
          class="flex items-center px-4 py-3.5 rounded-xl hover:bg-gray-50 group transition-all duration-300"
        >
          <span
            class="flex items-center justify-center w-10 h-10 rounded-lg bg-gray-100 text-gray-700 group-hover:bg-gray-200 transition-all duration-300"
          >
            <i class="fas fa-user"></i>
          </span>
          <span class="ml-3 font-medium text-gray-700 group-hover:text-gray-900"
            >My Info</span
          >
        </a>
      </li>

      <!-- My Orders -->
      <li>
        <a
          href="/order"
          class="flex items-center px-4 py-3.5 rounded-xl hover:bg-gray-50 group transition-all duration-300"
        >
          <span
            class="flex items-center justify-center w-10 h-10 rounded-lg bg-gray-100 text-gray-700 group-hover:bg-gray-200 transition-all duration-300"
          >
            <i class="fas fa-box"></i>
          </span>
          <span class="ml-3 font-medium text-gray-700 group-hover:text-gray-900"
            >My Orders</span
          >
        </a>
      </li>

      <!-- My Wallet -->
      <li>
        <a
          href="/wallet"
          class="flex items-center px-4 py-3.5 rounded-xl hover:bg-gray-50 group transition-all duration-300"
        >
          <span
            class="flex items-center justify-center w-10 h-10 rounded-lg bg-gray-100 text-gray-700 group-hover:bg-gray-200 transition-all duration-300"
          >
            <i class="fas fa-wallet"></i>
          </span>
          <span class="ml-3 font-medium text-gray-700 group-hover:text-gray-900"
            >My Wallet</span
          >
        </a>
      </li>
    </ul>
  </nav>

  <!-- Sign Out Button - Keeping original onclick -->
  <div class="absolute bottom-6 left-0 w-full px-4">
    <a
      href="/logout"
      onclick="confirmLogoutWithSadModal()"
      class="flex items-center px-4 py-3.5 rounded-xl bg-white border border-red-100 text-red-500 hover:bg-red-50 group transition-all duration-300"
    >
      <span
        class="flex items-center justify-center w-10 h-10 rounded-lg bg-red-50 text-red-500 group-hover:bg-red-100 transition-all duration-300"
      >
        <i class="fas fa-sign-out-alt"></i>
      </span>
      <span class="ml-3 font-medium">Sign Out</span>
    </a>
  </div>
</aside>

<script>
  function confirmLogoutWithSadModal() {
    event.preventDefault();
    Swal.fire({
      title: '<span class="text-xl font-medium">We\'ll Miss You!</span>',
      html:
        '<div class="mt-2">' +
        '<p class="text-gray-600">Are you sure you want to log out?</p>' +
        '<p class="text-gray-500 text-sm mt-1">Please come back soon!</p>' +
        '</div>',
      icon: 'warning',
      showCancelButton: true,
      confirmButtonColor: '#EAB308',
      cancelButtonColor: '#6B7280',
      confirmButtonText: 'Yes, log me out',
      cancelButtonText: 'Stay logged in',
      customClass: {
        popup: 'rounded-xl shadow-2xl',
        title: 'font-medium',
        htmlContainer: 'text-center',
      },
    }).then((result) => {
      if (result.isConfirmed) {
        window.location.href = '/logout';
      }
    });
  }
</script>
