<script>
    tailwind.config = {
      theme: {
        extend: {
          fontFamily: {
            sans: ['Inter', 'sans-serif'],
          },
        },
      },
    };
  </script>
  <!-- Sidebar -->
  <aside class="w-64 bg-slate-50 fixed left-0 top-[4rem] bottom-[4rem] shadow-lg overflow-y-auto z-1">
  
  <!-- Profile Section -->
  <div class="flex flex-col items-center py-8 border-b border-slate-200">
    <!-- Profile Image -->
    <div class="w-20 h-20 rounded-lg bg-gradient-to-br from-slate-700 to-slate-900 flex items-center justify-center">
      <i class="fas fa-user text-slate-100 text-3xl"></i>
    </div>
    <!-- User Info -->
    <h3 class="mt-4 text-xl font-bold text-slate-800"><%= user.name %></h3>
    <p class="text-sm font-medium text-slate-600"><%= user.email %></p>
  </div>

  <!-- Navigation Menu -->
  <nav class="mt-6 px-4">
    <ul class="space-y-2">
      <!-- My Info -->
      <li>
        <a href="/profile" class="flex items-center px-4 py-3 rounded-md bg-white shadow-sm">
          <i class="fas fa-user w-5 text-slate-700"></i>
          <span class="ml-3 font-medium text-slate-900">My Info</span>
        </a>
      </li>

      <!-- My Orders -->
      <li>
        <a href="/order" class="flex items-center px-4 py-3 rounded-md bg-white shadow-sm">
          <i class="fas fa-box w-5 text-slate-700"></i>
          <span class="ml-3 font-medium text-slate-900">My Orders</span>
        </a>
      </li>

      <!-- My Wallet -->
      <li>
        <a href="#" class="flex items-center px-4 py-3 rounded-md bg-white shadow-sm">
          <i class="fas fa-wallet w-5 text-slate-700"></i>
          <span class="ml-3 font-medium text-slate-900">My Wallet</span>
        </a>
      </li>

      <!-- Coupons -->
      <li>
        <a href="#" class="flex items-center px-4 py-3 rounded-md bg-white shadow-sm">
          <i class="fas fa-ticket-alt w-5 text-slate-700"></i>
          <span class="ml-3 font-medium text-slate-900">Coupons</span>
        </a>
      </li>
    </ul>
  </nav>

  <!-- Sign Out Button -->
  <div class="absolute bottom-6 left-0 w-full px-4">
    <a href="/logout" onclick="confirmLogoutWithSadModal()" 
       class="flex items-center px-4 py-3 rounded-md bg-red-50 text-red-700">
      <i class="fas fa-sign-out-alt w-5"></i>
      <span class="ml-3 font-medium">Sign Out</span>
    </a>
  </div>
</aside>

<!-- Content Placeholder -->
<!-- <div class="ml-64 mt-[4rem] mb-[4rem] p-6">
  <h1 class="text-2xl font-bold text-gray-800">Main Content Area</h1>
  <p class="text-gray-600 mt-2">Your page content goes here.</p>
</div> -->


<script>
  // Logout Confirmation Modal
  function confirmLogoutWithSadModal() {
    event.preventDefault();
    Swal.fire({
      title: 'We’re sad to see you go!',
      text: "Are you sure you want to log out? Please come back soon.",
      icon: 'info',
      showCancelButton: true,
      confirmButtonColor: '#3085d6',
      cancelButtonColor: '#d33',
      confirmButtonText: 'Yes, log me out',
      cancelButtonText: 'Stay logged in',
    }).then((result) => {
      if (result.isConfirmed) {
        window.location.href = '/logout';
      }
    });
  }
</script>
